# Maxichax-configArch
My personal arch config
## Startship+zsh
```bash
sudo pacman -S zsh && sudo pacman -S starship
```
## Hyprland
### hyprland shortcuts depedencies
screenshot
```bash
mkdir ~/Images/Screenshots/ && sudo pacman -S wc-clipboard && sudo pacman -S grimblast
```
### Waybar dependencies
wifi menu + bluetooth
```bash
sudo pacman -S rofi && git clone https://github.com/zbaylin/rofi-wifi-menu.git ~/.config/rofi/rofi-wifi-menu.sh && sudo pacman -S blueman && sudo pacman -S 
```
